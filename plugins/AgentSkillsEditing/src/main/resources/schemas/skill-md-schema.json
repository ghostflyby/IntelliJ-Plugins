{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/ghostflyby/IntelliJ-Plugins/main/plugins/AgentSkillsEditing/src/main/resources/schemas/skill-md-schema.json",
  "title": "SKILL.md Front-Matter Schema",
  "description": "JSON Schema for Codex SKILL.md YAML front-matter metadata",
  "type": "object",
  "properties": {
    "name": {
      "description": "Skill name in kebab-case. Must match the parent directory name.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$"
    },
    "description": {
      "description": "One-line description of the skill's purpose and usage.",
      "type": "string",
      "minLength": 1
    }
  },
  "required": ["name", "description"],
  "additionalProperties": false
}
